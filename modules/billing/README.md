# modules/billing

Logica de negocio del dominio. Estructura al implementarse (patron POS): `api.ts` (queries/mutations Supabase), `hooks.ts` (TanStack Query), `schemas.ts` (zod), `store.ts` (Zustand, solo si hay estado cliente). Catalogo de features del modulo: `docs/02-catalogo-funcionalidades.md`.
